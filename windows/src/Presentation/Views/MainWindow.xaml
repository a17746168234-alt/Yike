<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title=""
        Width="1220" Height="760" MinWidth="980" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResize"
        AllowsTransparency="True"
        FontFamily="Microsoft YaHei UI" FontSize="13"
        Background="Transparent"
        Foreground="{DynamicResource InkBrush}">
 <shell:WindowChrome.WindowChrome>
  <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="32" UseAeroCaptionButtons="False"/>
 </shell:WindowChrome.WindowChrome>
 <Window.Resources>
  <SolidColorBrush x:Key="WindowBrush" Color="#F3F6FC"/>
  <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF"/>
  <SolidColorBrush x:Key="InkBrush" Color="#172033"/>
  <SolidColorBrush x:Key="MutedBrush" Color="#697386"/>
  <SolidColorBrush x:Key="LineBrush" Color="#DDE5F0"/>
  <SolidColorBrush x:Key="BackdropBrush" Color="#EDF2F9"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="#FCFDFF"/>
  <SolidColorBrush x:Key="ControlBrush" Color="#F1F4F9"/>
  <SolidColorBrush x:Key="AccentBrush" Color="#625BF6"/>
  <SolidColorBrush x:Key="AccentHoverBrush" Color="#5149E8"/>
  <SolidColorBrush x:Key="SoftAccentBrush" Color="#ECEBFF"/>
  <SolidColorBrush x:Key="DangerBrush" Color="#E5484D"/>
  <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
   <GradientStop Color="#756CFF" Offset="0"/>
   <GradientStop Color="#4D7CFE" Offset="1"/>
  </LinearGradientBrush>

  <Style TargetType="Button">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="BorderBrush" Value="Transparent"/>
   <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="Padding" Value="12,8"/>
   <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="HorizontalContentAlignment" Value="Center"/>
   <Setter Property="VerticalContentAlignment" Value="Center"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="Button">
      <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
       <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
      </Border>
      <ControlTemplate.Triggers>
       <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="#18808CA0"/></Trigger>
       <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/></Trigger>
       <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter TargetName="ButtonBorder" Property="BorderThickness" Value="1"/></Trigger>
       <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.38"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>

  <Style x:Key="HeaderButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Height" Value="42"/>
   <Setter Property="Padding" Value="13,8"/>
   <Setter Property="Margin" Value="4,0"/>
   <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/>
   <Setter Property="BorderThickness" Value="1"/>
  </Style>
  <Style x:Key="WindowCaptionButton" TargetType="Button">
   <Setter Property="Width" Value="46"/>
   <Setter Property="Height" Value="48"/>
   <Setter Property="Margin" Value="0"/>
   <Setter Property="Padding" Value="0"/>
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="Cursor" Value="Arrow"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="Button">
      <Border x:Name="CaptionSurface" Background="{TemplateBinding Background}">
       <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>
      <ControlTemplate.Triggers>
       <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CaptionSurface" Property="Background" Value="{DynamicResource ControlBrush}"/></Trigger>
       <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsMouseOver" Value="True"/><Condition Property="Tag" Value="caption-close"/></MultiTrigger.Conditions><Setter TargetName="CaptionSurface" Property="Background" Value="#E81123"/><Setter Property="Foreground" Value="White"/></MultiTrigger>
       <Trigger Property="IsPressed" Value="True"><Setter TargetName="CaptionSurface" Property="Opacity" Value="0.72"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style x:Key="ToolbarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Height" Value="40"/>
   <Setter Property="Padding" Value="8,7"/>
   <Setter Property="Margin" Value="2,0"/>
   <Setter Property="Background" Value="Transparent"/>
  </Style>
  <Style x:Key="ToolbarContextMenu" TargetType="ContextMenu">
   <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/>
   <Setter Property="BorderThickness" Value="1"/>
   <Setter Property="Padding" Value="6"/>
   <Setter Property="HasDropShadow" Value="True"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="ContextMenu">
      <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
       <Border.Effect><DropShadowEffect BlurRadius="18" ShadowDepth="5" Direction="270" Opacity="0.24" Color="#111827"/></Border.Effect>
       <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
      </Border>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style x:Key="ToolbarMenuItem" TargetType="MenuItem">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="Padding" Value="12,9"/>
   <Setter Property="Margin" Value="0,1"/>
   <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="MenuItem">
      <Border x:Name="MenuSurface" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
       <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
      </Border>
      <ControlTemplate.Triggers>
       <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="MenuSurface" Property="Background" Value="{DynamicResource SoftAccentBrush}"/></Trigger>
       <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Background" Value="{StaticResource AccentGradient}"/>
   <Setter Property="Foreground" Value="White"/>
   <Setter Property="Height" Value="46"/>
   <Setter Property="MinWidth" Value="178"/>
   <Setter Property="Padding" Value="22,10"/>
   <Setter Property="FontWeight" Value="SemiBold"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="Button">
      <Border x:Name="PrimaryBorder" Background="{TemplateBinding Background}" CornerRadius="14" Padding="{TemplateBinding Padding}">
       <Border.Effect><DropShadowEffect BlurRadius="16" ShadowDepth="5" Direction="270" Opacity="0.22" Color="#625BF6"/></Border.Effect>
       <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>
      <ControlTemplate.Triggers>
       <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="PrimaryBorder" Property="Background" Value="{DynamicResource AccentHoverBrush}"/></Trigger>
       <Trigger Property="IsPressed" Value="True"><Setter TargetName="PrimaryBorder" Property="Opacity" Value="0.78"/></Trigger>
       <Trigger Property="IsEnabled" Value="False"><Setter TargetName="PrimaryBorder" Property="Background" Value="{DynamicResource ControlBrush}"/><Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/><Setter Property="Opacity" Value="0.7"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>

  <Style TargetType="ComboBox">
   <Setter Property="MinWidth" Value="132"/>
   <Setter Property="Height" Value="38"/>
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="VerticalContentAlignment" Value="Center"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="ComboBox">
      <Grid>
       <ToggleButton IsChecked="{Binding IsDropDownOpen,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}" Focusable="False" Foreground="{DynamicResource InkBrush}">
        <ToggleButton.Template>
         <ControlTemplate TargetType="ToggleButton">
           <Border x:Name="ComboBorder" Background="{DynamicResource ControlBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="12,5">
           <Grid>
            <ContentPresenter Content="{Binding SelectionBoxItem,RelativeSource={RelativeSource AncestorType=ComboBox}}" Margin="0,0,23,0" VerticalAlignment="Center"/>
            <TextBlock Text="&#xE70D;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
           </Grid>
          </Border>
          <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></Trigger></ControlTemplate.Triggers>
         </ControlTemplate>
        </ToggleButton.Template>
       </ToggleButton>
       <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
        <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="10" Padding="6" MinWidth="150">
         <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto"><StackPanel IsItemsHost="True"/></ScrollViewer>
        </Border>
       </Popup>
      </Grid>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style TargetType="ComboBoxItem">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="Padding" Value="10,8"/>
   <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
  </Style>
  <Style TargetType="TextBox">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/>
   <Setter Property="CaretBrush" Value="{DynamicResource InkBrush}"/>
   <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
  </Style>
  <Style TargetType="Expander"><Setter Property="Foreground" Value="{DynamicResource InkBrush}"/></Style>
  <Style TargetType="CheckBox">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="VerticalContentAlignment" Value="Center"/>
   <Setter Property="Margin" Value="0,8"/>
   <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="CheckBox">
      <StackPanel Orientation="Horizontal">
       <Border x:Name="CheckBoxSurface" Background="{DynamicResource ControlBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="6" Width="21" Height="21" Margin="0,0,10,0">
        <TextBlock x:Name="CheckMark" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" Foreground="White" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
       </Border>
       <ContentPresenter VerticalAlignment="Center"/>
      </StackPanel>
      <ControlTemplate.Triggers>
       <Trigger Property="IsChecked" Value="True"><Setter TargetName="CheckBoxSurface" Property="Background" Value="{DynamicResource AccentBrush}"/><Setter TargetName="CheckBoxSurface" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/></Trigger>
       <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style TargetType="Slider">
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="Slider">
      <Grid Height="28">
       <Track x:Name="PART_Track">
        <Track.DecreaseRepeatButton><RepeatButton Command="Slider.DecreaseLarge"><RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="{DynamicResource AccentBrush}" Height="5" CornerRadius="3"/></ControlTemplate></RepeatButton.Template></RepeatButton></Track.DecreaseRepeatButton>
        <Track.IncreaseRepeatButton><RepeatButton Command="Slider.IncreaseLarge"><RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="{DynamicResource ControlBrush}" Height="5" CornerRadius="3"/></ControlTemplate></RepeatButton.Template></RepeatButton></Track.IncreaseRepeatButton>
        <Track.Thumb><Thumb Width="22" Height="22"><Thumb.Template><ControlTemplate TargetType="Thumb"><Ellipse Fill="{DynamicResource PanelBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2"/></ControlTemplate></Thumb.Template></Thumb></Track.Thumb>
       </Track>
      </Grid>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
  <Style TargetType="RadioButton"><Setter Property="Foreground" Value="{DynamicResource InkBrush}"/></Style>
  <Style TargetType="PasswordBox">
   <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
   <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/>
   <Setter Property="CaretBrush" Value="{DynamicResource InkBrush}"/>
  </Style>
  <Style x:Key="BackdropStyle" TargetType="Border"><Setter Property="Background" Value="{DynamicResource BackdropBrush}"/></Style>
  <Style x:Key="TranslationSurface" TargetType="Border"><Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/></Style>
 </Window.Resources>

 <Border x:Name="MainWindowFrame" CornerRadius="32" Background="{DynamicResource WindowBrush}" ClipToBounds="True">
 <Grid ClipToBounds="True">
  <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="80"/><RowDefinition Height="*"/><RowDefinition Height="34"/></Grid.RowDefinitions>
  <Border Grid.RowSpan="4" Style="{StaticResource BackdropStyle}" IsHitTestVisible="False"/>
  <Grid Grid.RowSpan="4" IsHitTestVisible="False" Opacity="0.42">
   <Ellipse Width="680" Height="460" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-290,-280,0,0">
    <Ellipse.Fill><RadialGradientBrush><GradientStop Color="#80756CFF" Offset="0"/><GradientStop Color="#00756CFF" Offset="1"/></RadialGradientBrush></Ellipse.Fill>
   </Ellipse>
   <Ellipse Width="620" Height="460" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-280,-260">
    <Ellipse.Fill><RadialGradientBrush><GradientStop Color="#615ECFFF" Offset="0"/><GradientStop Color="#005ECFFF" Offset="1"/></RadialGradientBrush></Ellipse.Fill>
   </Ellipse>
  </Grid>

  <Border x:Name="CustomTitleBar" Grid.Row="0" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Background="#08000000">
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
    <Button x:Name="MinimizeWindowButton" Style="{StaticResource WindowCaptionButton}" ToolTip="最小化" Tag="caption-minimize"><TextBlock Text="&#xE921;" FontFamily="Segoe Fluent Icons" FontSize="10"/></Button>
    <Button x:Name="MaximizeWindowButton" Style="{StaticResource WindowCaptionButton}" ToolTip="最大化" Tag="caption-maximize"><TextBlock x:Name="MaximizeWindowGlyph" Text="&#xE922;" FontFamily="Segoe Fluent Icons" FontSize="10"/></Button>
    <Button x:Name="CloseWindowButton" Style="{StaticResource WindowCaptionButton}" ToolTip="关闭" Tag="caption-close"><TextBlock Text="&#xE8BB;" FontFamily="Segoe Fluent Icons" FontSize="10"/></Button>
   </StackPanel>
  </Border>

  <Border Grid.Row="1" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Background="#0AFFFFFF">
   <Grid Margin="28,0,24,0">
    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
     <Border Width="48" Height="48" CornerRadius="15" Background="Transparent" Margin="0,0,14,0">
      <Image x:Name="AppIcon" Width="40" Height="40" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
     </Border>
     <StackPanel VerticalAlignment="Center">
      <TextBlock x:Name="AppDisplayName" Text="Yike" FontSize="21" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="300"/>
      <Border Background="{DynamicResource SoftAccentBrush}" CornerRadius="8" Padding="7,3" Margin="0,5,0,0" HorizontalAlignment="Left">
       <StackPanel Orientation="Horizontal">
        <ContentControl x:Name="EngineStatusIcon" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="EngineStatus" Text="请选择赠送额度或个人密钥" FontSize="10.5" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/>
       </StackPanel>
      </Border>
     </StackPanel>
    </StackPanel>
    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
     <Button x:Name="EngineButton" Style="{StaticResource HeaderButton}"/>
     <Button x:Name="HistoryButton" Style="{StaticResource HeaderButton}"/>
     <Border Width="1" Height="24" Background="{DynamicResource LineBrush}" Margin="9,0"/>
     <Button x:Name="SettingsButton" Style="{StaticResource HeaderButton}" ToolTip="设置" Width="44" Height="40" Padding="0"/>
    </StackPanel>
   </Grid>
  </Border>

  <Border Grid.Row="2" Margin="24,18,24,10" CornerRadius="26" Background="#245B67F1" IsHitTestVisible="False">
   <Border.Effect><DropShadowEffect BlurRadius="34" ShadowDepth="10" Direction="270" Opacity="0.18" Color="#343B8F"/></Border.Effect>
  </Border>
  <Border Grid.Row="2" Margin="24,16,24,12" CornerRadius="26" Style="{StaticResource TranslationSurface}" BorderThickness="1">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="64"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="22,0">
     <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="64"/><ColumnDefinition/></Grid.ColumnDefinitions>
     <Grid>
      <ComboBox x:Name="SourceLanguage" HorizontalAlignment="Left" VerticalAlignment="Center"/>
      <TextBlock x:Name="SourceHeading" Text="原文" FontSize="19" FontWeight="SemiBold" Foreground="{DynamicResource InkBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
     </Grid>
     <Button x:Name="SwapButton" Grid.Column="1" ToolTip="交换语言" Width="42" Height="38" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center">
      <TextBlock Text="&#xE8AB;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource AccentBrush}"/>
     </Button>
     <Grid Grid.Column="2">
      <TextBlock x:Name="TargetHeading" Text="译文" FontSize="19" FontWeight="SemiBold" Foreground="{DynamicResource InkBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
      <ComboBox x:Name="TargetLanguage" HorizontalAlignment="Right" VerticalAlignment="Center"/>
     </Grid>
    </Grid>

    <Grid Grid.Row="1">
     <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="1"/><ColumnDefinition/></Grid.ColumnDefinitions>
     <Border BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,1,0,1">
      <Grid x:Name="SourcePane" AllowDrop="True">
       <Grid.RowDefinitions><RowDefinition/><RowDefinition Height="34"/></Grid.RowDefinitions>
       <TextBox x:Name="SourceText" Margin="24,20,24,6" BorderThickness="0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontSize="16" MaxLength="5000"/>
       <StackPanel x:Name="SourcePlaceholder" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False" Margin="0,-8,0,0">
        <Border Width="56" Height="56" CornerRadius="18" Background="{DynamicResource SoftAccentBrush}" HorizontalAlignment="Center">
         <TextBlock Text="&#xE8D4;" FontFamily="Segoe Fluent Icons" FontSize="23" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <TextBlock Text="输入或粘贴需要翻译的内容" FontSize="14" FontWeight="SemiBold" Margin="0,13,0,0" HorizontalAlignment="Center"/>
        <TextBlock Text="也可以拖入图片，或使用下方的语音与截图工具" FontSize="11" Margin="0,6,0,0" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Center"/>
       </StackPanel>
       <Image x:Name="SourceImage" Stretch="Uniform" Margin="12" Visibility="Collapsed"/>
       <Border x:Name="SourceImageHint" Visibility="Collapsed" IsHitTestVisible="False" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,14,0" Padding="10,6" CornerRadius="9" Background="{DynamicResource SoftAccentBrush}"><TextBlock Text="单击打开原图" Foreground="{DynamicResource AccentBrush}" FontSize="11"/></Border>
       <TextBlock x:Name="CharacterCount" Grid.Row="1" Text="0 / 5000" HorizontalAlignment="Right" Margin="0,0,22,0" VerticalAlignment="Center" Foreground="{DynamicResource MutedBrush}" FontSize="10"/>
      </Grid>
     </Border>
     <Border Grid.Column="1" Background="{DynamicResource LineBrush}"/>
     <Border Grid.Column="2" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,1,0,1">
      <Grid>
       <TextBox x:Name="ResultText" Margin="24,20" BorderThickness="0" IsReadOnly="False" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontSize="16" ToolTip="译文可直接编辑或删除"/>
       <StackPanel x:Name="ResultPlaceholder" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False" Margin="0,-8,0,0">
        <Border Width="56" Height="56" CornerRadius="18" Background="{DynamicResource SoftAccentBrush}" HorizontalAlignment="Center">
         <TextBlock Text="&#xE8C1;" FontFamily="Segoe Fluent Icons" FontSize="23" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <TextBlock Text="译文将在这里逐行呈现" FontSize="14" FontWeight="SemiBold" Margin="0,13,0,0" HorizontalAlignment="Center"/>
        <TextBlock Text="翻译后可直接编辑或删除" FontSize="11" Margin="0,6,0,0" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Center"/>
       </StackPanel>
       <Image x:Name="ResultImage" Stretch="Uniform" Margin="12" Visibility="Collapsed"/>
       <Border x:Name="ResultImageHint" Visibility="Collapsed" IsHitTestVisible="False" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,14,0" Padding="10,6" CornerRadius="9" Background="{DynamicResource SoftAccentBrush}"><TextBlock Text="单击打开译图" Foreground="{DynamicResource AccentBrush}" FontSize="11"/></Border>
      </Grid>
     </Border>
    </Grid>

    <Grid Grid.Row="2" Margin="14,7" MinHeight="56">
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
      <Button x:Name="VoiceButton" Style="{StaticResource ToolbarButton}"/>
      <Button x:Name="ImageButton" Style="{StaticResource ToolbarButton}"/>
      <Button x:Name="CaptureButton" Style="{StaticResource ToolbarButton}"/>
      <Button x:Name="OcrButton" Style="{StaticResource ToolbarButton}"/>
      <Button x:Name="ClearButton" Style="{StaticResource ToolbarButton}" IsEnabled="False"/>
     </WrapPanel>
     <Button x:Name="TranslateButton" Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="开始翻译    Enter" Margin="8,0" HorizontalAlignment="Center" VerticalAlignment="Center" IsEnabled="False"/>
     <WrapPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
      <Button x:Name="ImageToolsButton" Style="{StaticResource ToolbarButton}" Visibility="Collapsed"/>
      <Button x:Name="SpeakButton" Style="{StaticResource ToolbarButton}" IsEnabled="False"/>
      <Button x:Name="PauseSpeechButton" Style="{StaticResource ToolbarButton}" Visibility="Collapsed"/>
      <Button x:Name="StopSpeechButton" Style="{StaticResource ToolbarButton}" Visibility="Collapsed"/>
      <Button x:Name="CopyButton" Style="{StaticResource ToolbarButton}" IsEnabled="False"/>
     </WrapPanel>
    </Grid>
   </Grid>
  </Border>

  <Grid Grid.Row="3" Margin="28,0">
   <TextBlock x:Name="Status" Text="Enter 翻译 · Shift+Enter 换行 · Ctrl+Shift+F 划词翻译" Foreground="{DynamicResource MutedBrush}" FontSize="11.5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
  </Grid>
 </Grid>
 </Border>
</Window>
