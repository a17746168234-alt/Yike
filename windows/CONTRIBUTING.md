# 参与开发

请先阅读 README 与 docs/代码结构.md。开发和验证需在 Windows x64 上完成；无需真实 API 密钥即可执行回归测试。

## 修改与验证

- 按现有模块划分添加代码。App 使用 partial class，请避免新增重复成员。
- 构建使用 .NET Framework 自带 C# 编译器，新增代码需兼容 C# 5。
- 单独说明功能行为变化，避免在格式整理中顺带修改界面或交互。
- 功能修改应验证受影响的行为；界面修改执行主题、缩放和预览检查。

```powershell
.\verify.ps1 -SkipRuntimes
```

PR 中说明问题、改动效果和验证结果；界面修改可附无个人信息的前后截图。不要提交生成目录、运行库、模型、账号数据、真实翻译历史或密钥。新增源码目录后同步更新 tools/export-source.ps1。

## 问题报告

说明 Windows 版本、应用版本、复现步骤、预期行为和实际行为。OCR 问题请说明相应 Windows OCR 语言是否已安装，语音问题请说明运行库与麦克风权限情况。请使用虚构或公开的示例文本。