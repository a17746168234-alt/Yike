# 更新记录

## 2.1

- “检查更新”改为读取 Yike 服务器维护的 Windows 版本清单，服务器暂时不可用时自动回退 GitHub Releases。
- Windows Release 标签兼容两段至四段版本号；2.1 使用 `windows-v2.1.0`，让已有 2.0 客户端也能发现更新，下载后核对大小和 SHA-256 再启动安装。
- 安装包不再内嵌 252 MB 的 Whisper small Q8 模型，体积从约 266 MB 降至约 18 MB；首次安装通过独立进度窗口下载并校验同一高精度模型，更新时复用本机模型，因此不降低语音识别质量。

- 语音输入统一使用 multilingual Whisper small Q8；固定中文、English 与自动检测不再分流到旧版 Windows 听写引擎。
- 实时识别使用更长上下文、beam search 与解码回退，停止后再对整段录音做一次高精度校正。
- 语音输入在正常说话后连续 6 秒未检测到声音时自动停止，完成整段校正后自动发送翻译；手动停止仍只保留文字，不会擅自提交。
- 语音输入按钮改为随麦克风音量持续起伏的动态波形；整段录音在最终识别前会抑制低频噪声并安全归一化音量，同时提高 beam / best-of 搜索宽度以改善中英文与自动检测的准确度。
- 主界面、设置、历史记录、图片预览与划词翻译等窗口统一采用 Windows 风格的右上角最小化、最大化 / 还原和关闭按钮，移除左上角红黄绿控制点。
- 安装器新增安装位置选择，可输入路径或浏览到其他本机磁盘；更新会沿用已登记目录，更换目录时安全迁移安装文件，并拒绝覆盖非空的普通文件夹。
- 卸载器改为从实际安装目录自迁移执行，自选磁盘或路径也可从 Windows“已安装的应用”完整卸载；主窗口、任务栏与划词小窗固定显示 Yike 图标，不再被用户头像替换。
- 头像下方的引擎标识会随选择显示 Yike 或 DeepL 图标；完整安装包移除未使用的旧语音模型和 Python 缓存 / 安装元数据，保留实际使用的 Whisper small Q8 与全部运行功能。

- “OCR / 框选”弹出菜单复用主界面的深浅色、圆角、阴影、悬停色与图标设计，不再显示突兀的系统白色菜单。
- 开始下一次翻译时自动停止并清理上一段暂停的朗读，隐藏“继续 / 停止”按钮并恢复初始朗读界面。
- 主界面 DeepL 入口改为双引擎选择菜单，可明确选择“DeepL 高质量翻译”的赠送额度或“DeepL（个人接入）”的自有密钥额度；选择会被记住，失败时不会自动切换并消耗另一种额度。
- 头像下方说明随当前引擎同步更新；设置中的“DeepL 密钥与帮助”更名为“了解与帮助”，新增两种 Windows 翻译引擎的数据流与额度说明。

## 1.2.4

- 恢复“账号与安全”页面，支持邮箱验证码注册、邮箱密码登录、额度刷新与退出登录。
- 登录会话通过 Windows DPAPI 加密保存在当前用户目录；密码和邮箱验证码不写入本机文件。
- 注册并登录后优先使用 Yike 公共 DeepL 体验额度；公共额度不可用或当前语言不受支持时，安全回退到个人 DeepL 密钥。
- 公共翻译按服务端限制分批请求，支持中文、英语、日语和韩语，并在翻译后同步剩余额度。
- “账号与安全”新增昵称与头像修改；资料按邮箱区分并通过 Windows DPAPI 加密保存在本机，不上传服务器。
- 设置窗口直接复用主界面的浅色/深色语义色板，消除相同主题下的色差。
- 主窗口的窗口控制改为与设置窗口相同的左上角红、黄、绿控制点。

## 1.2.3

- 修复检查更新只读取本地文件而误报最新版的问题，改为查询 GitHub 的 Windows 稳定版，排除 macOS、草稿、预发布和未完成上传的安装包。
- 更新对话框显示实际安装版本和最新发布版本；支持取消下载，校验大小和 SHA-256 后安装并重新启动，保留设置与历史。
- Windows 安装登记版本从实际程序读取，避免版本号散落在 UI 和安装器中。
- 设置已打开时，DeepL 帮助等入口正确切换页面；各窗口标题区按钮不再误触发拖动。
- 权限页可刷新检测快捷键、麦克风、Whisper、Windows 语音组件和 OCR 语言；快捷键页支持重新注册。
- 朗读设置中的暂停与停止按实际播放状态启用；修正空格语音输入与托盘退出说明。

## 1.2.2

- 划词翻译小窗右上角新增关闭按钮。
- 点击标题区按钮图标时不会触发窗口拖动，窄窗口下副标题自动省略。

## 1.2.1

- 修复离线语音识别输出在管道缓冲中等待，导致短句不及时显示的问题。
- 缩小实时识别的音频上下文，停止时保留有界的最后一句识别时间；未识别到文字时给出提示。
- 麦克风就绪后更新监听状态；缩短 Windows 识别的句末等待。
- 朗读首段生成后立即播放，后续段落同时生成，保留完整文本、音色、语速和暂停/停止行为。
- 在线朗读首段并行尝试代理与直连，后续段落优先使用已成功的连接方式。

## 仓库整理

- 整理公开仓库说明、构建与发布流程、隐私说明和第三方来源。
- 添加 GitHub Windows 编译与离线回归测试工作流。
- 构建脚本支持不带语音运行库的核心构建，移除本机备份路径依赖。
- 更新配置与 MSIX 发布身份改为示例地址或构建参数。

## 1.2.0

- Windows x64 桌面翻译，支持 DeepL、划词、截图 / 图片 OCR、朗读、语音输入和历史。
- 支持浅色、深色和跟随系统主题。
- 当前源码设置界面已移除“账号与安全”页面。
